<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <LinkTo @route="situation" class="navbar-brand">Danger Room</LinkTo>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="#">Situation <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link disabled" href="#">Collection</a>
      </li>
    </ul>
    
    {{#if session.isAuthenticated}}
      <span class="navbar-text">
        {{session.data.authenticated.profile.email}}
      </span>
      <form class="form-inline my-2 my-lg-0">
        <button {{action this.logout }} class="btn btn-outline-success my-2 my-sm-0" type="submit">Logout</button>
      </form>
    {{else}}
      <form class="form-inline my-2 my-lg-0">
        <button {{action this.login}} class="btn btn-outline-success my-2 my-sm-0" type="submit">Login</button>
      </form>
    {{/if}}
  </div>
</nav>
